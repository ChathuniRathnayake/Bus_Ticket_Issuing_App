import { Typography } from '@mui/material';

function DashboardHome() {
  return (
    <div>
      <Typography variant="h4">Admin Dashboard</Typography>
      <p>Welcome! Manage buses, routes, and more.</p>
      {/* Add links to buses, etc. */}
    </div>
  );
}

export default DashboardHome;